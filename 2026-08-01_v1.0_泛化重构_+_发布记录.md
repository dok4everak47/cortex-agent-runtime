# 2026-08-01 v1.0 泛化重构 + 发布记录

# 2026-08-01 v1.0 泛化重构 + 发布记录

## 决策:laravel-ai-agent → cortex-agent-runtime

**动机**:原项目是 Laravel 专用 MCP server,想叫 "Agent Runtime / Framework" 但能力只有 Laravel,名不副实。选择**泛化升级**而非纯改名:core + domains 双层架构,Laravel 变 domain。

**架构**(TASK-GENERALIZE.md 驱动,OpenCode 实现):
- `src/core/`:registry(域注册制)、detector(项目类型检测)、glob
- `src/domains/laravel/`:原 24 工具 + workflows + planner + security 整体搬迁,行为不变
- `src/domains/generic/`:gitStatus / fileSearch / projectTree(语言无关,任何项目加载)
- env:`CORTEX_PROJECT_PATH` 优先,`LARAVEL_PROJECT_PATH` 兜底

**验证**:273 单测全绿 + 6 golden 端到端(真实 Laravel 项目跑 CRUD/评论/API/debug)+ tsc + 双域冒烟。

**发布**:npm `cortex-agent-runtime@1.0.0-beta.1`(beta tag),旧包 `laravel-ai-agent@0.9.1` 已 deprecate。GitHub `dok4everak47/cortex-agent-runtime`(301 跳转)。opencode.jsonc mcp 条目 `laravel` → `cortex`。

## 踩坑(已更新到 mcp-server-development 技能)

1. **OpenCode run 非交互模式读外部目录会被 auto-reject**(~/.config 等)——消费端配置必须划为外部步骤
2. **Agent Supervisor 遗留 workflow 状态锁 planner 只读角色**——删 .supervisor/workflow.json 或 `--pure` 跑
3. **Magic Context 检测到 DCP 插件/compaction.auto=true 会自禁**——已移除 @tarquinen/opencode-dcp,跨会话记忆恢复
4. **工具数**:README 工具表截断导致 TASK 里写错(25 vs 实际 24)——以 tool-registry 源码为准

## 后续

- [ ] v1.0 正式版(去掉 beta):先跑真实项目功能,再发 latest
- [ ] 简历项目名同步为 Cortex Agent Runtime

